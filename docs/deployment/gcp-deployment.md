# Google Cloud Platform Deployment

> Автоматический деплой Currency Bot на Google Compute Engine VM через GitHub Actions

## 📋 Содержание

- [Предварительные требования](#предварительные-требования)
- [Настройка GCP](#настройка-gcp)
- [Настройка GitHub Secrets](#настройка-github-secrets)
- [Процесс деплоя](#процесс-деплоя)
- [Troubleshooting](#troubleshooting)

---

## Предварительные требования

### На GCP VM

VM должна быть подготовлена:

1. **Docker установлен:**
   ```bash
   # Проверка
   docker --version

   # Если нет - установка
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh

   # Добавить пользователя в группу docker (опционально)
   # Это позволит использовать docker без sudo
   sudo usermod -aG docker $USER
   # Перелогиниться для применения изменений
   ```

   **Примечание:** CI/CD скрипт использует `sudo docker`, поэтому добавление пользователя в группу docker опционально.

2. **Открыт порт 8080** (если нужен внешний доступ):
   ```bash
   # В GCP Console: VPC Network → Firewall Rules
   # Создать правило для входящего трафика на порт 8080
   ```

3. **SSH доступ настроен:**
   ```bash
   # gcloud должен иметь доступ к VM
   gcloud compute ssh <VM_NAME> --zone=<ZONE>
   ```

---

## Настройка GCP

### 1. Создать Service Account

В GCP Console:

1. Перейти в **IAM & Admin** → **Service Accounts**
2. Нажать **Create Service Account**
3. Указать имя: `github-actions-deployer`
4. Выбрать роли:
   - `Compute Instance Admin (v1)` - для управления VM
   - `Service Account User` - для подключения к VM

### 2. Создать JSON ключ

1. Выбрать созданный Service Account
2. **Keys** → **Add Key** → **Create new key**
3. Тип: **JSON**
4. Скачать файл (например, `github-actions-deployer.json`)

**⚠️ Важно:** Этот файл содержит приватный ключ! Не комитить в Git!

### 3. Настроить VM

Убедиться, что VM:
- Имеет внешний IP (или Cloud NAT для доступа к GHCR)
- Может пуллить образы из `ghcr.io`
- Имеет достаточно места для Docker образов

```bash
# Проверить свободное место
df -h

# Проверить что Docker работает
docker ps
```

---

## Настройка GitHub Secrets

В GitHub репозитории: **Settings** → **Secrets and variables** → **Actions**

### Обязательные секреты:

| Secret | Описание | Пример |
|--------|----------|--------|
| `GCC_TOKEN` | JSON ключ от Service Account | `{"type":"service_account",...}` |
| `GCC_VM_NAME` | Имя VM в GCP | `currency-bot-vm` |
| `GCC_VM_ZONE` | Зона где находится VM | `us-central1-a` |
| `BOT_TOKEN` | Токен Telegram бота | `7746884457:AAE33aVh3...` |
| `CHAT_ID` | ID чата для отправки сообщений | `123456789` |

### Как добавить `GCC_TOKEN`:

1. Открыть скачанный JSON файл в текстовом редакторе
2. Скопировать **весь JSON** (включая фигурные скобки)
3. В GitHub Secrets создать новый секрет `GCC_TOKEN`
4. Вставить содержимое JSON файла
5. **Удалить локальный JSON файл!**

### Как узнать `GCC_VM_NAME` и `GCC_VM_ZONE`:

```bash
# Список всех VM
gcloud compute instances list

# Вывод:
# NAME              ZONE           MACHINE_TYPE  INTERNAL_IP  EXTERNAL_IP    STATUS
# currency-bot-vm   us-central1-a  e2-micro      10.128.0.2   34.123.45.67   RUNNING
```

---

## Процесс деплоя

### Автоматический деплой

При push в `main` или `master`:

```bash
git add .
git commit -m "feat: update bot logic"
git push origin main
```

**Что происходит:**

1. ✅ **Build** - собирается JAR файл
2. ✅ **Docker** - создаётся Docker образ и пушится в GHCR
3. ✅ **Deploy** - подключается к GCP VM и разворачивает сервис

### Просмотр логов деплоя

GitHub → **Actions** → выбрать workflow run → **Deploy to GCP VM**

### Ручной деплой

Можно запустить вручную через GitHub Actions:

1. GitHub → **Actions**
2. Выбрать workflow **CI/CD with GHCR Publishing**
3. **Run workflow** → выбрать branch → **Run workflow**

---

## Что делает деплой

Скрипт выполняет на VM следующие шаги:

```bash
# 1. Логин в GitHub Container Registry
echo $GITHUB_TOKEN | docker login ghcr.io -u $GITHUB_ACTOR --password-stdin

# 2. Пуллинг последнего образа
docker pull ghcr.io/username/repo:latest

# 3. Остановка старого контейнера
docker stop currency-bot || true
docker rm currency-bot || true

# 4. Запуск нового контейнера
docker run -d \
  --name currency-bot \
  --restart unless-stopped \
  -v /var/log/currency-bot:/app/logs \
  -e BOT_TOKEN=$BOT_TOKEN \
  -e CHAT_ID=$CHAT_ID \
  ghcr.io/username/repo:latest

# 5. Проверка статуса
docker ps | grep currency-bot
```

---

## Проверка работы

### После деплоя

1. **Проверить логи GitHub Actions:**
   ```
   ✅ Successfully deployed to currency-bot-vm
   🔍 Checking container status...
   Up 2 seconds
   ```

2. **Зайти на VM и проверить:**
   ```bash
   gcloud compute ssh currency-bot-vm --zone=us-central1-a

   # Проверить контейнер
   docker ps | grep currency-bot

   # Посмотреть логи
   docker logs -f currency-bot

   # Или через volume
   tail -f /var/log/currency-bot/currency-bot.log
   ```

3. **Проверить что бот работает:**
   - Отправить сообщение боту в Telegram
   - Проверить что бот отвечает

---

## Troubleshooting

### Ошибка: Permission denied (SSH)

**Проблема:** gcloud не может подключиться к VM

**Решение:**
```bash
# Проверить что Service Account имеет роль Compute Instance Admin
# Проверить что VM существует и запущена
gcloud compute instances list

# Попробовать подключиться вручную
gcloud compute ssh currency-bot-vm --zone=us-central1-a
```

### Ошибка: permission denied while trying to connect to the Docker daemon

**Проблема:** Пользователь на VM не имеет прав на использование Docker

**Решение:**

CI/CD уже использует `sudo docker`, поэтому эта ошибка не должна возникать. Если возникает:

```bash
# Зайти на VM
gcloud compute ssh your-vm-name --zone=your-zone

# Проверить что Docker работает с sudo
sudo docker ps

# Если нужно использовать docker без sudo (опционально):
sudo usermod -aG docker $USER
# Перелогиниться
exit
# Зайти снова
gcloud compute ssh your-vm-name --zone=your-zone
# Теперь docker должен работать без sudo
docker ps
```

### Ошибка: Docker login failed

**Проблема:** VM не может залогиниться в GHCR

**Решение:**
```bash
# Проверить что VM имеет доступ к интернету
ping ghcr.io

# Проверить что GITHUB_TOKEN валиден
# В GitHub Actions этот токен создаётся автоматически

# Попробовать залогиниться вручную на VM
echo "YOUR_GITHUB_TOKEN" | sudo docker login ghcr.io -u YOUR_USERNAME --password-stdin
```

### Ошибка: Container fails to start

**Проблема:** Контейнер не запускается

**Решение:**
```bash
# Зайти на VM
gcloud compute ssh currency-bot-vm --zone=us-central1-a

# Посмотреть логи контейнера
docker logs currency-bot

# Проверить переменные окружения
docker inspect currency-bot | grep -A10 Env

# Запустить контейнер вручную для дебага
docker run -it --rm \
  -e BOT_TOKEN=$BOT_TOKEN \
  -e CHAT_ID=$CHAT_ID \
  ghcr.io/username/repo:latest
```

### Ошибка: Image pull failed

**Проблема:** VM не может скачать образ из GHCR

**Решение:**
```bash
# Проверить что образ существует
# GitHub → Packages → найти образ

# Проверить что образ публичный или VM залогинена
docker login ghcr.io -u YOUR_USERNAME

# Попробовать пуллить вручную
docker pull ghcr.io/username/repo:latest
```

### Логи деплоя не показывают ошибку

**Проблема:** Деплой "успешен", но бот не работает

**Решение:**
```bash
# Зайти на VM и проверить всё вручную
gcloud compute ssh currency-bot-vm --zone=us-central1-a

# Статус контейнера
docker ps -a | grep currency-bot

# Полные логи
docker logs currency-bot --tail 100

# Логи из volume
tail -100 /var/log/currency-bot/currency-bot.log
```

---

## Безопасность

### ✅ DO

- Использовать Service Account с минимальными правами
- Хранить все секреты в GitHub Secrets
- Регулярно ротировать Service Account ключи
- Использовать приватные Docker образы в GHCR
- Настроить firewall rules для ограничения доступа к VM

### ❌ DON'T

- Не комитить JSON ключи в Git
- Не хранить секреты в коде
- Не давать Service Account лишние права
- Не оставлять порты открытыми без необходимости

---

## Связанные документы

- [CI/CD Guide](ci-cd-guide.md) - общий гайд по CI/CD
- [Docker Volumes](docker-volumes.md) - сохранение логов
- [Troubleshooting](../troubleshooting/common-issues.md) - частые проблемы

---

**Последнее обновление:** 2025-11-30
