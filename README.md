# Currency Bot

[![CI/CD](https://github.com/proflyder/tg-bot-currency/actions/workflows/ci-cd.yml/badge.svg)](https://github.com/proflyder/tg-bot-currency/actions/workflows/ci-cd.yml)
[![Tests](https://img.shields.io/badge/tests-passing-brightgreen)](https://github.com/proflyder/tg-bot-currency/actions/workflows/ci-cd.yml)
[![Kotlin](https://img.shields.io/badge/Kotlin-2.2.20-blue?logo=kotlin)](https://kotlinlang.org)
[![Ktor](https://img.shields.io/badge/Ktor-3.3.2-orange?logo=ktor)](https://ktor.io)

Telegram бот для отслеживания курсов валют с сайта kurs.kz. Бот автоматически отправляет обновления курсов доллара и рубля к тенге каждый час.

## 📚 Документация

Полная документация доступна в [`docs/`](docs/index.md):

- **[Быстрый старт](docs/guides/quickstart.md)** - запусти бота за 5 минут
- **[Logging Guide](docs/guides/logging.md)** - best practices логирования
- **[Docker Volumes](docs/deployment/docker-volumes.md)** - сохранение логов при редеплое
- **[CI/CD Guide](docs/deployment/ci-cd-guide.md)** - автоматизация через GitHub Actions
- **[Scripts](docs/scripts/readme.md)** - утилиты для работы с проектом

➡️ **[Полный индекс документации](docs/index.md)**

### 🤖 Для Claude Code
- **[Agent Onboarding](docs/AGENT-ONBOARDING.md)** - полный контекст проекта для Claude

## Технологический стек

- **Kotlin** - язык программирования
- **Ktor Server** - фреймворк для веб-приложений (CIO engine)
- **Ktor Client** - HTTP клиент для запросов
- **H2 Database** + **Exposed ORM** - хранение истории курсов
- **Quartz Scheduler** - планирование задач по cron расписанию
- **Koin** - dependency injection
- **Ksoup** - парсер HTML
- **Kotlinx Serialization** - сериализация JSON
- **Kotlinx Coroutines** - асинхронное программирование
- **Clean Architecture** - архитектурный паттерн
- **Docker** - контейнеризация

## Архитектура проекта

Проект построен на основе Clean Architecture:

```
src/main/kotlin/com/tgbot/
├── domain/
│   ├── model/           # Entities (модели данных)
│   ├── repository/      # Интерфейсы репозиториев
│   └── usecase/         # Бизнес-логика (Use Cases)
├── data/
│   ├── repository/      # Реализации репозиториев
│   ├── remote/          # API клиенты
│   │   ├── telegram/    # Telegram Bot API
│   │   └── parser/      # Парсер kurs.kz
│   └── dto/             # Data Transfer Objects
├── di/                  # Dependency Injection
└── scheduler/           # Планировщик задач
```

## Настройка

### 1. Получение токена Telegram бота

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота

### 2. Получение Chat ID

1. Добавьте бота в нужный чат или группу
2. Отправьте любое сообщение боту
3. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `chat.id` в ответе

### 3. Конфигурация

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните значения:

```env
BOT_TOKEN=ваш_токен_бота
CHAT_ID=ваш_chat_id
```

## Запуск

### Локальный запуск

```bash
# Сборка проекта
./gradlew build

# Запуск приложения
./gradlew run
```

### Запуск через Docker Compose

Docker Compose автоматически определяет режим работы на основе переменной `DOCKER_IMAGE` в `.env`:

#### Локальная разработка (сборка из исходников)

```bash
# 1. Создай .env файл
cp .env.example .env
nano .env  # Укажи BOT_TOKEN и CHAT_ID

# 2. Убедись что DOCKER_IMAGE закомментирована в .env
# DOCKER_IMAGE=ghcr.io/...  <- должна быть закомментирована

# 3. Запуск с локальной сборкой
docker-compose up --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

**Что происходит:**
- Docker собирает образ из исходников через Dockerfile
- Образ называется `currency-bot:local`
- Используется для разработки и тестирования

#### Продакшн (использование готового образа из GHCR) 🚀

**Это самый простой способ!** GitHub Actions автоматически собирает и публикует образ при каждом push.

```bash
# 1. Создай .env файл
cp .env.example .env
nano .env

# 2. Раскомментируй и укажи DOCKER_IMAGE в .env:
DOCKER_IMAGE=ghcr.io/ваш-username/currency-bot:latest

# 3. Скачать образ и запустить
docker-compose pull
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

**Что происходит:**
- Docker скачивает готовый образ из GitHub Container Registry
- Локальная сборка пропускается (образ уже готов)
- Используется для продакшна

#### Обновление при новом коммите

```bash
# GitHub Actions автоматически опубликует новую версию
# Просто обнови образ и перезапусти:
docker-compose pull
docker-compose up -d
```

#### Ручной запуск через Docker (без compose)

```bash
# Локальная сборка
docker build -t currency-bot .
docker run -d \
  --name currency-bot \
  -e BOT_TOKEN=ваш_токен \
  -e CHAT_ID=ваш_chat_id \
  -e DATABASE_PATH=/app/data/currency-history \
  -p 8080:8080 \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/data:/app/data \
  currency-bot

# Или использование образа из GHCR
docker pull ghcr.io/ваш-username/currency-bot:latest
docker run -d \
  --name currency-bot \
  -e BOT_TOKEN=xxx \
  -e CHAT_ID=yyy \
  -e DATABASE_PATH=/app/data/currency-history \
  -p 8080:8080 \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/data:/app/data \
  ghcr.io/ваш-username/currency-bot:latest
```

## Тестирование

Проект покрыт автоматическими тестами с использованием современных best practices.

### Запуск тестов локально

```bash
# Запустить все тесты
./gradlew test

# Запустить конкретный тест
./gradlew test --tests "SendCurrencyRatesUseCaseTest"

# Запустить тесты с отчетом
./gradlew test
# Отчет будет доступен в: build/reports/tests/test/index.html
```

### Тестовое покрытие

- **50+ тестов** покрывают основную функциональность
- **Unit тесты** с использованием MockK для изоляции компонентов
- **Integration тесты** для CurrencyHistoryRepository (реальные файловые операции)
- **Koin DI тесты** для проверки корректности dependency injection

#### Покрытые компоненты:

| Компонент | Тесты | Описание |
|-----------|-------|----------|
| **SendCurrencyRatesUseCase** | 10 тестов | Успешные сценарии, обработка ошибок, граничные случаи |
| **CurrencyHistoryRepository** | 14 тестов | Сохранение, очистка, thread safety, файловые операции |
| **CurrencyRepositoryImpl** | 3 теста | Делегирование парсеру, проброс ошибок |
| **TelegramRepositoryImpl** | 11 тестов | Отправка сообщений, форматирование, обработка ошибок |
| **AppModule (Koin DI)** | 12 тестов | Конфигурация, резолвинг зависимостей, singleton scope |

### Используемые технологии

- **JUnit 5** - фреймворк для тестирования
- **MockK** - мощная библиотека моков для Kotlin
- **Koin Test** - тестирование dependency injection
- **Kotlinx Coroutines Test** - тестирование асинхронного кода
- **Kotest Assertions** - выразительные ассерты

### CI/CD интеграция

Тесты автоматически запускаются при каждом push и pull request через GitHub Actions:

- ✅ Блокируют сборку и деплой при падении
- 📊 Публикуют test reports как GitHub Checks
- 📦 Сохраняют результаты как artifacts (30 дней)
- 🔍 Видны напрямую в Pull Requests

## CI/CD

Проект использует GitHub Actions для автоматического тестирования, сборки и публикации Docker образов в GitHub Container Registry (GHCR).

### Dockerfile

Проект использует **два Dockerfile**:

- **`Dockerfile`** - для локальной разработки
  - Multi-stage build с Gradle
  - Собирает JAR из исходников
  - Используется в `docker-compose up --build`

- **`Dockerfile.ci`** - для CI/CD
  - Копирует готовый JAR из artifacts
  - Без повторной сборки
  - Быстрее и эффективнее в CI

### GitHub Actions Workflow

Pipeline состоит из четырёх последовательных джоб:

#### 1. Test Job ✅
- Checkout кода
- Настройка Java 21 и Gradle
- **Запуск всех тестов**: `./gradlew test`
- Публикация test reports как GitHub Checks
- Сохранение test results как artifacts (30 дней)
- **Блокирует следующие джобы при падении тестов**

#### 2. Build Job 🔨
- Запускается только после успешного прохождения тестов
- Checkout кода
- Настройка Java 21 и Gradle
- Сборка проекта: `./gradlew buildFatJar`
- Сохранение JAR как артефакт

#### 3. Docker Job 🐳
- Зависит от успешного завершения Test и Build джоб
- Скачивание JAR артефакта из Build джобы
- Сборка Docker образа через `Dockerfile.ci`
- **Публикация образа в GHCR** (GitHub Container Registry)
- Образ доступен по адресу: `ghcr.io/ваш-username/currency-bot:latest`

#### 4. Deploy Job 🚀
- **Автоматический деплой на Google Cloud VM**
- Подключается к GCP через gcloud CLI
- Разворачивает контейнер на VM с persistent volumes:
  - `/var/log/currency-bot:/app/logs` - логи приложения
  - `/var/data/currency-bot:/app/data` - JSON история курсов
- Запускается только на main/master ветке
- Выполняет health check после деплоя

### Автоматическая публикация

При каждом push в main/master:
1. GitHub Actions собирает проект
2. Создаёт Docker образ
3. Публикует его в GHCR по адресу `ghcr.io/ваш-username/currency-bot:latest`
4. Образ становится доступен публично для скачивания

### Настройка автоматического деплоя на GCP

**Необходимые GitHub Secrets** (Settings → Secrets and variables → Actions):

| Secret | Описание | Как получить |
|--------|----------|--------------|
| `GCC_TOKEN` | JSON ключ Service Account | GCP Console → IAM & Admin → Service Accounts |
| `GCC_VM_NAME` | Имя VM | `gcloud compute instances list` |
| `GCC_VM_ZONE` | Зона VM | Например: `us-central1-a` |
| `BOT_TOKEN` | Токен Telegram бота | @BotFather в Telegram |
| `CHAT_ID` | ID чата для сообщений | См. [docs/guides/get-chat-id.md](docs/guides/get-chat-id.md) |

**Подробная инструкция:** [docs/deployment/gcp-deployment.md](docs/deployment/gcp-deployment.md)

## Логирование

Проект использует продвинутую систему логирования с:
- **Структурированными логами** (JSON для продакшна)
- **Автоматической ротацией** (30 дней, макс 1GB)
- **MDC контекстом** для трейсинга
- **Маскированием** sensitive данных

### Просмотр логов

**Утилита для работы с логами (рекомендуется):**
```bash
./scripts/logs.sh tail      # Следить за логами в реальном времени
./scripts/logs.sh show      # Показать последние 100 строк
./scripts/logs.sh save      # Сохранить логи в файл
./scripts/logs.sh backup    # Создать backup архив
./scripts/logs.sh size      # Показать размер логов
./scripts/logs.sh clean 30  # Удалить логи старше 30 дней
./scripts/logs.sh help      # Полный список команд
```

**Или напрямую через Docker:**
```bash
# Логи в реальном времени
docker logs -f currency-bot

# Или из файла (если используешь volume)
docker exec currency-bot tail -f /app/logs/currency-bot.log
```

**Локально:**
```bash
tail -f logs/currency-bot.log
```

### Сохранение логов при редеплое

Логи автоматически сохраняются через **Docker Volume**. После редеплоя (stop/start контейнера) логи не теряются.

**Пример редеплоя:**
```bash
docker-compose down    # Остановить
docker-compose pull    # Скачать новую версию
docker-compose up -d   # Запустить (логи сохранятся!)
```

Подробнее:
- [Logging Best Practices](docs/guides/logging.md)
- [Docker Volumes Guide](docs/deployment/docker-volumes.md)

## Настройка интервала обновлений

По умолчанию бот отправляет обновления каждый час. Чтобы изменить интервал, отредактируйте `src/main/resources/application.yaml`:

```yaml
scheduler:
  intervalHours: 1  # Измените на нужное значение
```

## Формат сообщений

Бот отправляет красиво отформатированные сообщения с использованием Markdown:

```
💱 Курсы валют на kurs.kz

🇺🇸 USD → KZT
  Покупка: 513.90 ₸
  Продажа: 516.30 ₸

🇷🇺 RUB → KZT
  Покупка: 6.49 ₸
  Продажа: 6.61 ₸
```

## Разработка

### Структура кода

- **Domain Layer** - бизнес-логика, независимая от фреймворков
- **Data Layer** - работа с внешними источниками данных
- **DI Layer** - управление зависимостями
- **Scheduler** - планирование периодических задач

### Добавление новых валютных пар

Отредактируйте `KursKzParser.kt` и добавьте нужные валютные пары в метод `extractRates()`.

