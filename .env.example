# ═══════════════════════════════════════════════════════════════════
# Конфигурация Currency Bot
# ═══════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────
# ОБЯЗАТЕЛЬНЫЕ переменные:
# ───────────────────────────────────────────────────────────────────

# Токен Telegram бота (получи у @BotFather)
BOT_TOKEN=your-bot-token-here

# ID чата для отправки сообщений (узнай через @userinfobot или getUpdates)
CHAT_ID=your-chat-id-here

# Root key от сервиса unkey
UNKEY_ROOT_KEY=your-unkey-root-key

# ───────────────────────────────────────────────────────────────────
# ОПЦИОНАЛЬНЫЕ переменные:
# ───────────────────────────────────────────────────────────────────

# Docker образ для использования
#
# Если НЕ указана (закомментирована):
#   → Docker соберёт образ локально из исходников
#   → Используй для разработки
#
# Если указана:
#   → Docker скачает готовый образ из GHCR
#   → Используй для продакшна
#
# Примеры:
# DOCKER_IMAGE=ghcr.io/stanislavplotnikov/currency-bot:latest
# DOCKER_IMAGE=ghcr.io/username/currency-bot:v1.0.0
# DOCKER_IMAGE=currency-bot:custom-tag


# ═══════════════════════════════════════════════════════════════════
# Приоритет переменных окружения (от высшего к низшему):
# ═══════════════════════════════════════════════════════════════════
# 1. Системные переменные (export BOT_TOKEN=xxx)
# 2. Docker -e флаги (docker run -e BOT_TOKEN=xxx)
# 3. Этот .env файл
# 4. Дефолтные значения в application.yaml
# ═══════════════════════════════════════════════════════════════════
